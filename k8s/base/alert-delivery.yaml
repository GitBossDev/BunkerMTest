apiVersion: apps/v1
kind: Deployment
metadata:
  name: bhm-alert-delivery
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: bhm-alert-delivery
  template:
    metadata:
      labels:
        app.kubernetes.io/name: bhm-alert-delivery
    spec:
      containers:
        - name: worker
          image: localhost/bhm-api:latest
          imagePullPolicy: Never
          envFrom:
            - secretRef:
                name: bhm-env
            - configMapRef:
                name: bhm-k8s-config
          command:
            - /bin/sh
            - -c
            - /opt/venv/bin/python -m services.alert_delivery_daemon --interval 5
          volumeMounts:
            - name: api-logs
              mountPath: /var/log/api
          startupProbe:
            exec:
              command:
                - /opt/venv/bin/python3
                - -c
                # Comprueba que el daemon escribio el heartbeat (ventana amplia para el arranque)
                - "import os,time; f='/tmp/alert_delivery.alive'; assert os.path.exists(f) and (time.time()-os.path.getmtime(f))<120"
            failureThreshold: 30
            periodSeconds: 10
          readinessProbe:
            exec:
              command:
                - /opt/venv/bin/python
                - -c
                - import sys; sys.path.insert(0, '/app'); from sqlalchemy import create_engine, text; from core.config import Settings; from core.database_url import get_sync_database_url; settings = Settings(); engine = create_engine(get_sync_database_url(settings.resolved_control_plane_database_url), future=True); connection = engine.connect(); connection.execute(text('SELECT 1')); connection.close()
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            exec:
              command:
                - /opt/venv/bin/python3
                - -c
                # Ventana mas amplia para tolerar ciclos lentos o errores transitorios de BD
                - "import os,time; f='/tmp/alert_delivery.alive'; assert os.path.exists(f) and (time.time()-os.path.getmtime(f))<120"
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 192Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: api-logs
          emptyDir: {}