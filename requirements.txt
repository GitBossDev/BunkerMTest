# requirements.txt - Broker Health Manager Utilities

# Core dependencies for scripts
psycopg[binary]>=3.2.1    # PostgreSQL adapter for current sync runtime and tests
psycopg2-binary>=2.9.9    # Legacy PostgreSQL adapter kept for migration scripts still using psycopg2
requests>=2.31.0           # HTTP library

# Optional for advanced features
paho-mqtt>=1.6.1          # MQTT client (for testing)
python-dotenv>=1.0.0      # Environment variables
python-multipart>=0.0.20  # FastAPI form/file uploads used by backend routers
